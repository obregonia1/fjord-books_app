<% provide(:title, 'All users') %>
<h1><%= User.model_name.human %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user %>
    </li>
  <% end %>
</ul>
